# claude-container

Quickest way to spin up Claude with `bypassPermissions` mode in Docker.

1. `docker build -t claude-container`

2. `. ./install.sh`

3. In the project directory, `claude_docker launch`

4. Inside the docker shell: `claude.sh`

That's it.